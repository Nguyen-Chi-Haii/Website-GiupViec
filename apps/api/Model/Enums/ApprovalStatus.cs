namespace GiupViecAPI.Model.Enums
{
    public enum ApprovalStatus
    {
        Pending = 1,    // Chờ phê duyệt
        Approved = 2,   // Đã phê duyệt (hiển thị trên hệ thống)
        Rejected = 3    // Bị từ chối
    }
}
